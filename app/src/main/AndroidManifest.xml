<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gurarsh_sidhu.lyrical">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LyricsActivity" />
        <activity android:name=".saved_lyrics" />

        <provider
            android:name=".SongProvider"
            android:authorities="com.example.gurarsh_sidhu.lyrical"
            android:exported="false" />

        <activity android:name=".LyricsActivity2"></activity>
    </application>

</manifest>